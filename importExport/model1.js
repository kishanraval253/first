const add = (a, b) => {
    return a + b;
}

const sub = (a, b) => {
    return a - b;
}

const name = "kishan raval";

// module.exports.add = add;
// module.exports.sub = sub;

module.exports = {add, sub}